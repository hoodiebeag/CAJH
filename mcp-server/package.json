{
  "type": "module",
  "name": "cajh-mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing cajh's source, backtests, decision journal, and a gated deploy path to an agent. Deliberately a separate package from the bot itself - it must never become a dependency the live process loads at runtime.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "dev": "tsc -p . --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
